meta {
  name: "Create Todo - Invalid Body"
  type: "http"
}

request {
  method: "POST"
  url: "http://localhost:8000/todos"

  header {
    name: "Content-Type"
    value: "application/json"
  }

  // Missing required "title" field to trigger validation error
  body: json {
    "description": "This request is missing the title field."
  }
}
