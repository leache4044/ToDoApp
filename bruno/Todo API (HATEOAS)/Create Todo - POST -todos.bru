meta {
  name: Create Todo - POST /todos
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/todos
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "title": "Write Bruno tests",
    "description": "Validate the Todo API via Bruno."
  }
}

settings {
  encodeUrl: true
}
